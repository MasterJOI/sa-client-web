// import {FormGroup, ValidationErrors, ValidatorFn} from '@angular/forms';
//
// export const roleFilledValidator: ValidatorFn = (
//   control: FormGroup
// ): ValidationErrors | null => {
//   const student = control.get('student').get('email');
//   const teacher = control.get('teacher').get('email');
//
//   if ((student.value && student.valid) || (other.value && other.valid)) {
//     return null; // null => valid
//   } else {
//     return { paymentSelectionInvalid: true }; // validation errors => not valid
//   }
// };
